name: Sync Upstream

on:
  workflow_dispatch:
    inputs:
      channel:
        description: "release or alpha"
        required: true
        default: "release"
        type: choice
        options:
          - release
          - alpha
  schedule:
    - cron: "*/10 * * * *"

permissions:
  contents: write
  actions: write

concurrency:
  group: sync-upstream-${{ github.event.inputs.channel || 'auto' }}
  cancel-in-progress: false

jobs:
  sync-release:
    if: ${{ github.event_name == 'schedule' || github.event.inputs.channel == 'release' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Configure git
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Add upstream
        run: |
          git remote add upstream https://github.com/MetaCubeX/mihomo.git || git remote set-url upstream https://github.com/MetaCubeX/mihomo.git
          git fetch upstream +refs/heads/Meta:refs/remotes/upstream/Meta --prune

      - name: Sync latest release
        id: sync
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          patch_dir="$(mktemp -d)"
          cp patches/*.patch "$patch_dir"/

          latest="$(curl -fsSL https://api.github.com/repos/MetaCubeX/mihomo/releases/latest | jq -r '.tag_name')"
          if [[ -z "$latest" || "$latest" == "null" ]]; then
            echo "No upstream latest release found" >&2
            exit 1
          fi
          git fetch upstream "+refs/tags/${latest}:refs/tags/upstream/${latest}"
          upstream_tag="refs/tags/upstream/${latest}"
          git merge-base --is-ancestor "${upstream_tag}" upstream/Meta || {
            echo "Upstream latest release ${latest} is not on the official Meta branch" >&2
            exit 1
          }

          current_meta="$(git ls-remote origin refs/heads/Meta | awk '{print $1}')"
          current_release_tag="$(git ls-remote origin "refs/tags/${latest}" | awk '{print $1}')"
          current_meta_tree=""
          current_release_tag_tree=""
          if [[ -n "$current_meta" ]]; then
            current_meta_tree="$(git ls-tree "$current_meta" | git mktree)"
          fi
          if [[ -n "$current_release_tag" ]]; then
            current_release_tag_tree="$(git ls-tree "$current_release_tag" | git mktree)"
          fi
          git switch -C "release/${latest}" "${upstream_tag}"
          git am "$patch_dir"/*.patch || {
            echo "Compatibility patch replay failed on ${latest}" >&2
            exit 1
          }
          patched_release_tree="$(git rev-parse HEAD^{tree})"
          if [[ "$patched_release_tree" == "$current_meta_tree" && "$patched_release_tree" == "$current_release_tag_tree" ]]; then
            echo "Latest release ${latest} already exists in fork and Meta/tag are current"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "version=${latest}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git push origin "HEAD:Meta" --force
          git push origin "HEAD:release/${latest}" --force
          git tag -f "${latest}"
          git push origin "refs/tags/${latest}" --force
          echo "changed=true" >> "$GITHUB_OUTPUT"
          echo "version=${latest}" >> "$GITHUB_OUTPUT"

      - name: Trigger release build
        if: ${{ steps.sync.outputs.changed == 'true' }}
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'build.yml',
              ref: 'main',
              inputs: {
                channel: 'release',
                version: '${{ steps.sync.outputs.version }}',
                build_ref: '${{ steps.sync.outputs.version }}',
              },
            });

  sync-alpha:
    if: ${{ github.event_name == 'schedule' || github.event.inputs.channel == 'alpha' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Configure git
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Add upstream
        run: |
          git remote add upstream https://github.com/MetaCubeX/mihomo.git || git remote set-url upstream https://github.com/MetaCubeX/mihomo.git
          git fetch upstream +refs/heads/Alpha:refs/remotes/upstream/Alpha --prune

      - name: Sync Alpha
        id: sync
        run: |
          set -euo pipefail
          patch_dir="$(mktemp -d)"
          cp patches/*.patch "$patch_dir"/

          upstream_alpha="$(git rev-parse upstream/Alpha)"
          current_alpha="$(git ls-remote origin refs/heads/Alpha | awk '{print $1}')"
          current_alpha_tag="$(git ls-remote origin refs/tags/Prerelease-Alpha | awk '{print $1}')"
          current_alpha_tree=""
          current_alpha_tag_tree=""
          if [[ -n "$current_alpha" ]]; then
            current_alpha_tree="$(git ls-tree "$current_alpha" | git mktree)"
          fi
          if [[ -n "$current_alpha_tag" ]]; then
            current_alpha_tag_tree="$(git ls-tree "$current_alpha_tag" | git mktree)"
          fi

          git switch -C Alpha upstream/Alpha
          git am "$patch_dir"/*.patch || {
            echo "Compatibility patch replay failed on Alpha" >&2
            exit 1
          }

          patched_alpha_tree="$(git rev-parse HEAD^{tree})"
          if [[ "$patched_alpha_tree" == "$current_alpha_tree" && "$patched_alpha_tree" == "$current_alpha_tag_tree" ]]; then
            echo "Fork Alpha branch/tag are already current"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git push origin HEAD:Alpha --force
          git tag -f Prerelease-Alpha
          git push origin refs/tags/Prerelease-Alpha --force
          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Trigger alpha build
        if: ${{ steps.sync.outputs.changed == 'true' }}
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'build.yml',
              ref: 'main',
              inputs: {
                channel: 'alpha',
                build_ref: 'Alpha',
              },
            });
